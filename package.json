{
  "name": "nakath-lk-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dispatch:reminders": "node scripts/dispatch-reminders.mjs",
    "setup:reminder-scheduler": "bash scripts/setup-reminder-scheduler.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "npm@10.2.4"
}
